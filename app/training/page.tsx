import ProtocolViewer from '@/components/ProtocolViewer'

export const metadata = {
  title: 'Clinical Protocols — Clinic A',
}

export default function TrainingPage() {
  return <ProtocolViewer />
}
